h1 コメント一覧

table
  thead
    tr
      th コメント先
      th = Comment.human_attribute_name(:content)

  tbody
    - @comments.each do |comment|
      tr
        td = link_to comment.commentable.title, comment.commentable
        td = comment.content
= paginate @comments
